# Development/Sandbox Environment
# This file is used for sandbox deployments
# API calls use relative paths which work through the NPMplus proxy

VITE_API_BASE_URL=/api

# Renders the "Sandbox" badge beside the version number in the header.
# Intentionally absent from .env.production — production shows no badge.
VITE_APP_ENV=sandbox
