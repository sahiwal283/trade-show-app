# Server Configuration
PORT=5000
NODE_ENV=production

# Database Configuration
DB_HOST=127.0.0.1
DB_PORT=5432
DB_NAME=expense_app
DB_USER=expense_user
DB_PASSWORD=change_me

# JWT Secret (min 32 chars random)
JWT_SECRET=replace_with_strong_random_secret

# File Upload
# Use an absolute path for production to store receipts persistently
UPLOAD_DIR=/var/lib/trade-show-app/uploads
MAX_FILE_SIZE=5242880

# CORS
# Set to your frontend origin, e.g. https://expense.example.com
CORS_ORIGIN=


