# Sandbox Environment Configuration Template
# This file documents the sandbox configuration for reference
# Actual sandbox env file location: /etc/expenseapp/backend.env (Container 203)

# ===========================================
# APPLICATION CONFIGURATION
# ===========================================
NODE_ENV=development
PORT=3000

# ===========================================
# SECURITY SECRETS (SANDBOX - NOT FOR PRODUCTION)
# ===========================================
JWT_SECRET=sandbox_jwt_secret_not_for_production
SESSION_SECRET=sandbox_session_secret_not_for_production

# ===========================================
# DATABASE CONFIGURATION (SANDBOX)
# ===========================================
DB_HOST=localhost
DB_PORT=5432
DB_NAME=expenseapp_sandbox
DB_USER=expenseapp_sandbox
DB_PASSWORD=<sandbox_db_password>

# ===========================================
# FRONTEND CONFIGURATION
# ===========================================
CORS_ORIGIN=https://sandbox.expenseapp.example.com
FRONTEND_URL=https://sandbox.expenseapp.example.com

# ===========================================
# ZOHO BOOKS API - SANDBOX/TEST CREDENTIALS
# ===========================================
# These are test/sandbox credentials - NEVER use in production

# Haute Entity (Sandbox - REAL API for testing)
ZOHO_CLIENT_ID=1000.PWO6LIXJ34P6SL4AULI2EJR4EGPHAA
ZOHO_CLIENT_SECRET=<sandbox_secret>
ZOHO_REFRESH_TOKEN=<sandbox_refresh_token>
ZOHO_ORGANIZATION_ID=856048585

# Zoho Account IDs (from sandbox Chart of Accounts)
ZOHO_EXPENSE_ACCOUNT_ID=5254962000000091710
ZOHO_PAID_THROUGH_ACCOUNT_ID=5254962000000129043

# Zoho API Base URLs
ZOHO_API_BASE_URL=https://www.zohoapis.com/books/v3
ZOHO_ACCOUNTS_BASE_URL=https://accounts.zoho.com/oauth/v2

# Account Names (fallback)
ZOHO_EXPENSE_ACCOUNT_NAME=Meals
ZOHO_PAID_THROUGH_ACCOUNT=Business Checking

# ===========================================
# MULTI-ENTITY ZOHO CONFIGURATION (SANDBOX)
# ===========================================
# Mock entities for testing multi-entity support

ZOHO_ALPHA_ENABLED=true
ZOHO_ALPHA_MOCK=true

ZOHO_BETA_ENABLED=true
ZOHO_BETA_MOCK=true

ZOHO_GAMMA_ENABLED=true
ZOHO_GAMMA_MOCK=true

ZOHO_DELTA_ENABLED=true
ZOHO_DELTA_MOCK=true

# ===========================================
# FILE UPLOAD CONFIGURATION
# ===========================================
MAX_FILE_SIZE=10485760
UPLOAD_DIR=/var/lib/expenseapp/uploads

# ===========================================
# OLLAMA LLM CONFIGURATION (SANDBOX ONLY)
# ===========================================
# Ollama Lite running in Container 302 (192.168.1.173)
# Used for low-confidence OCR field enhancement
OLLAMA_API_URL=http://192.168.1.173:11434
OLLAMA_MODEL=dolphin-llama3
OLLAMA_TEMPERATURE=0.1
OLLAMA_TIMEOUT=30000

# ===========================================
# LOGGING CONFIGURATION
# ===========================================
LOG_LEVEL=debug

# ===========================================
# IMPORTANT NOTES
# ===========================================
# 1. This is SANDBOX configuration - for testing only
# 2. Production must use completely different credentials
# 3. Sandbox Organization ID: 856048585 (nabeelhpe@gmail.com)
# 4. Production will use different Organization ID
# 5. Never copy sandbox secrets to production

# ========== MIDAS EXPENSE ENGINE (sandbox / cutover) ==========
# MIDAS_MODE=disabled|mock|live
# Local Ext: MIDAS_MODE=live, MIDAS_BASE_URL=http://localhost:4000/api/v1
# CT sandbox: set live + midas after ext:create-connection key is issued
MIDAS_MODE=disabled
# MIDAS_BASE_URL=http://localhost:4000/api/v1
# MIDAS_API_KEY=
# MIDAS_WEB_BASE_URL=http://localhost:5173
MIDAS_TIMEOUT_MS=120000
# EXPENSE_BACKEND=local|dual|midas
EXPENSE_BACKEND=local
